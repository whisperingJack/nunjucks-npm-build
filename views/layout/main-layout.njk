<!DOCTYPE html>

<html>

    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="{{ data.meta_description }}" />
        <meta name="keywords" content="{{ data.meta_keywords }}" />
        <meta name="robots" content="index, follow" />

        <!-- Keyword opportunity -->
        <title>{{ data.title_tag }}</title>

        <!-- SEO -->
        <link rel="canonical" href="{{data.canonical_link}}" />

        <!-- Favicon -->
        <link rel="icon" href="favicon.ico"/>

        <!-- Fonts -->
        {% include '../partials/head/fonts.njk' %}

        <!-- Icons -->
        {% include '../partials/head/icons.njk' %}

        <!-- jQuery -->
        <script defer src="js/jquery.min.js"></script>

        <!-- CSS -->
        {% include '../partials/head/css.njk' %}

        <!-- Captcha -->
        <script defer  src='https://www.google.com/recaptcha/api.js'></script>

        <!--Structured Data -->  
        {% include '../partials/head/structured_data.njk' %}

    </head>

    <body>
        
        {% block header %}
        {% endblock %}
    
        {% block content %}
        {% endblock %}
        
        {% include 'partials/body/footer.njk' %}
        
    </body>

        <!--Javascript -->  
        {% include '../partials/head/javascript.njk' %}
    
</html>